import math
def imprimeLogaritmo(x):
    if x <= 0:
      print ("Solo numeros positivos, por favor.")
      return
    result = math.log(x)
    print (f"El log de x es, {result}")

imprimeLogaritmo(10)
